<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="Cue.Pages.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:Cue.Pages"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Background="{ThemeResource LayerFillColorDefaultBrush}"
    FontFamily="{StaticResource CueFontFamily}"
    mc:Ignorable="d">

    <Page.Resources>
        <Style x:Key="SettingsNavItemStyle" TargetType="ListViewItem">
            <Setter Property="MinHeight" Value="36" />
            <Setter Property="Margin" Value="0,1" />
            <Setter Property="Padding" Value="10,0" />
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
            <Setter Property="CornerRadius" Value="{StaticResource CueRadiusSmall}" />
            <Setter Property="FocusVisualMargin" Value="2" />
            <Setter Property="FocusVisualPrimaryThickness" Value="1" />
            <Setter Property="FocusVisualSecondaryThickness" Value="0" />
        </Style>

        <Style x:Key="SettingsNavTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="{StaticResource CueFontRowSub}" />
            <Setter Property="Foreground" Value="{ThemeResource TextFillColorPrimaryBrush}" />
            <Setter Property="VerticalAlignment" Value="Center" />
        </Style>

        <Style x:Key="SettingsNavGlyphStyle" TargetType="FontIcon">
            <Setter Property="Width" Value="18" />
            <Setter Property="FontSize" Value="15" />
            <Setter Property="Foreground" Value="{ThemeResource TextFillColorSecondaryBrush}" />
        </Style>

        <Style x:Key="SettingsLabelStyle" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="{StaticResource CueFontFamilySemiBold}" />
            <Setter Property="FontSize" Value="{StaticResource CueFontRowSub}" />
            <Setter Property="Foreground" Value="{ThemeResource TextFillColorPrimaryBrush}" />
        </Style>

        <Style x:Key="SettingsCaptionStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="{StaticResource CueFontSecondary}" />
            <Setter Property="Foreground" Value="{ThemeResource TextFillColorSecondaryBrush}" />
            <Setter Property="TextWrapping" Value="Wrap" />
        </Style>

        <Style x:Key="SettingsCardStyle" TargetType="Border">
            <Setter Property="Padding" Value="16" />
            <Setter Property="CornerRadius" Value="{StaticResource CueRadiusCard}" />
            <Setter Property="Background" Value="{ThemeResource CardBackgroundFillColorDefaultBrush}" />
            <Setter Property="BorderBrush" Value="{ThemeResource CardStrokeColorDefaultBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="BackgroundSizing" Value="InnerBorderEdge" />
        </Style>

        <Style x:Key="SettingsDividerStyle" TargetType="Border">
            <Setter Property="Height" Value="1" />
            <Setter Property="Background" Value="{ThemeResource DividerStrokeColorDefaultBrush}" />
            <Setter Property="Margin" Value="0,2" />
        </Style>

        <Style x:Key="SettingsSectionTitleStyle" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="{StaticResource CueFontFamilySemiBold}" />
            <Setter Property="FontSize" Value="{StaticResource CueFontSection}" />
            <Setter Property="Foreground" Value="{ThemeResource TextFillColorPrimaryBrush}" />
            <Setter Property="Margin" Value="0,0,0,2" />
        </Style>
    </Page.Resources>

    <Grid Padding="28,20" RowSpacing="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Style="{StaticResource CuePageTitleTextStyle}" Text="설정" />

        <Grid Grid.Row="1" ColumnSpacing="24">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="176" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Border
                VerticalAlignment="Top"
                Padding="5"
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                BackgroundSizing="InnerBorderEdge"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="{StaticResource CueRadiusCard}">
                <ListView
                    x:Name="SectionNav"
                    HorizontalAlignment="Stretch"
                    Background="Transparent"
                    IsItemClickEnabled="False"
                    ItemContainerStyle="{StaticResource SettingsNavItemStyle}"
                    SelectionChanged="SectionNav_SelectionChanged"
                    SelectionMode="Single"
                    SelectedIndex="0">
                    <ListView.Resources>
                        <SolidColorBrush x:Key="ListViewItemBackgroundSelected" Color="{ThemeResource SubtleFillColorSecondary}" />
                        <SolidColorBrush x:Key="ListViewItemBackgroundSelectedPointerOver" Color="{ThemeResource SubtleFillColorSecondary}" />
                        <SolidColorBrush x:Key="ListViewItemBackgroundPointerOver" Color="{ThemeResource SubtleFillColorSecondary}" />
                        <SolidColorBrush x:Key="ListViewItemBackgroundPressed" Color="{ThemeResource SubtleFillColorTertiary}" />
                        <SolidColorBrush x:Key="ListViewItemForegroundSelected" Color="{ThemeResource TextFillColorPrimary}" />
                        <SolidColorBrush x:Key="ListViewItemForegroundSelectedPointerOver" Color="{ThemeResource TextFillColorPrimary}" />
                    </ListView.Resources>
                    <ListViewItem Tag="Time">
                        <Grid ColumnSpacing="10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <FontIcon Glyph="&#xE823;" Style="{StaticResource SettingsNavGlyphStyle}" />
                            <TextBlock Grid.Column="1" Style="{StaticResource SettingsNavTextStyle}" Text="시간" />
                        </Grid>
                    </ListViewItem>
                    <ListViewItem Tag="Parsing">
                        <Grid ColumnSpacing="10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <FontIcon Glyph="&#xE943;" Style="{StaticResource SettingsNavGlyphStyle}" />
                            <TextBlock Grid.Column="1" Style="{StaticResource SettingsNavTextStyle}" Text="파싱" />
                        </Grid>
                    </ListViewItem>
                    <ListViewItem Tag="Appearance">
                        <Grid ColumnSpacing="10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <FontIcon Glyph="&#xE790;" Style="{StaticResource SettingsNavGlyphStyle}" />
                            <TextBlock Grid.Column="1" Style="{StaticResource SettingsNavTextStyle}" Text="외관" />
                        </Grid>
                    </ListViewItem>
                    <ListViewItem Tag="Notifications">
                        <Grid ColumnSpacing="10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <FontIcon Glyph="&#xE7F4;" Style="{StaticResource SettingsNavGlyphStyle}" />
                            <TextBlock Grid.Column="1" Style="{StaticResource SettingsNavTextStyle}" Text="알림" />
                        </Grid>
                    </ListViewItem>
                </ListView>
            </Border>

            <ScrollViewer
                Grid.Column="1"
                HorizontalScrollBarVisibility="Disabled"
                VerticalScrollBarVisibility="Auto">
                <Grid MaxWidth="680" HorizontalAlignment="Left">
                    <StackPanel
                        x:Name="TimeSection"
                        Spacing="12"
                        Visibility="Visible">
                        <TextBlock Style="{StaticResource SettingsSectionTitleStyle}" Text="시간" />

                        <Border Style="{StaticResource SettingsCardStyle}">
                            <StackPanel Spacing="16">
                                <Grid MinHeight="64" ColumnSpacing="20">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="320" />
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Spacing="5" VerticalAlignment="Center">
                                        <TextBlock Style="{StaticResource SettingsLabelStyle}" Text="주 시작 요일" />
                                        <TextBlock Style="{StaticResource SettingsCaptionStyle}" Text="주간 보기와 달력 선택에서 먼저 오는 요일" />
                                    </StackPanel>
                                    <ComboBox
                                        x:Name="FirstDayCombo"
                                        Grid.Column="1"
                                        Width="320"
                                        VerticalAlignment="Center"
                                        SelectionChanged="FirstDayCombo_SelectionChanged" />
                                </Grid>

                                <Border Style="{StaticResource SettingsDividerStyle}" />

                                <Grid MinHeight="64" ColumnSpacing="20">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="320" />
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Spacing="5" VerticalAlignment="Center">
                                        <TextBlock Style="{StaticResource SettingsLabelStyle}" Text="기본 시간대" />
                                        <TextBlock Style="{StaticResource SettingsCaptionStyle}" Text="오늘, 앞으로 할 일, 빠른 입력 날짜 기준. 다음 실행부터 전체 반영" />
                                    </StackPanel>
                                    <ComboBox
                                        x:Name="TimeZoneCombo"
                                        Grid.Column="1"
                                        Width="320"
                                        VerticalAlignment="Center"
                                        SelectionChanged="TimeZoneCombo_SelectionChanged" />
                                </Grid>
                            </StackPanel>
                        </Border>
                    </StackPanel>

                    <StackPanel
                        x:Name="ParsingSection"
                        Spacing="12"
                        Visibility="Collapsed">
                        <TextBlock Style="{StaticResource SettingsSectionTitleStyle}" Text="파싱" />

                        <Border Style="{StaticResource SettingsCardStyle}">
                            <StackPanel Spacing="16">
                                <Grid MinHeight="64" ColumnSpacing="20">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="320" />
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Spacing="5" VerticalAlignment="Center">
                                        <TextBlock Style="{StaticResource SettingsLabelStyle}" Text="1-6시를 오후로 해석" />
                                        <TextBlock Style="{StaticResource SettingsCaptionStyle}" Text="오전/오후가 없을 때 3시는 15:00으로 저장" />
                                    </StackPanel>
                                    <ToggleSwitch
                                        x:Name="AutoAfternoonSwitch"
                                        Grid.Column="1"
                                        MinWidth="96"
                                        HorizontalAlignment="Right"
                                        VerticalAlignment="Center"
                                        Toggled="AutoAfternoonSwitch_Toggled" />
                                </Grid>
                            </StackPanel>
                        </Border>

                        <Border Style="{StaticResource SettingsCardStyle}">
                            <StackPanel Spacing="16">
                                <TextBlock Style="{StaticResource CueCardHeaderTextStyle}" Text="커스텀 날짜 의미" />
                                <ListView
                                    x:Name="CustomDatesList"
                                    MaxHeight="220"
                                    SelectionMode="None">
                                    <ListView.ItemTemplate>
                                        <DataTemplate x:DataType="local:CustomDateMeaningRow">
                                            <Grid Padding="0,7" ColumnSpacing="12">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*" />
                                                    <ColumnDefinition Width="Auto" />
                                                </Grid.ColumnDefinitions>
                                                <StackPanel Spacing="2">
                                                    <TextBlock
                                                        FontSize="{StaticResource CueFontRowSub}"
                                                        Text="{x:Bind Name}" />
                                                    <TextBlock Style="{StaticResource SettingsCaptionStyle}" Text="{x:Bind DayCaption}" />
                                                </StackPanel>
                                                <Button
                                                    Grid.Column="1"
                                                    Padding="8,4"
                                                    Tag="{x:Bind Name}"
                                                    Click="RemoveCustomDate_Click"
                                                    Content="삭제"
                                                    Style="{StaticResource CueSubtleTextButtonStyle}" />
                                            </Grid>
                                        </DataTemplate>
                                    </ListView.ItemTemplate>
                                </ListView>
                                <Grid ColumnSpacing="8">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="128" />
                                        <ColumnDefinition Width="74" />
                                    </Grid.ColumnDefinitions>
                                    <TextBox x:Name="CustomDateNameBox" PlaceholderText="예: 월급날" />
                                    <NumberBox
                                        x:Name="CustomDateDayBox"
                                        Grid.Column="1"
                                        Minimum="1"
                                        Maximum="31"
                                        PlaceholderText="일"
                                        SpinButtonPlacementMode="Inline" />
                                    <Button
                                        Grid.Column="2"
                                        HorizontalAlignment="Stretch"
                                        Click="AddCustomDate_Click"
                                        Content="추가" />
                                </Grid>
                            </StackPanel>
                        </Border>
                    </StackPanel>

                    <StackPanel
                        x:Name="AppearanceSection"
                        Spacing="12"
                        Visibility="Collapsed">
                        <TextBlock Style="{StaticResource SettingsSectionTitleStyle}" Text="외관" />

                        <Border Style="{StaticResource SettingsCardStyle}">
                            <Grid MinHeight="64" ColumnSpacing="20">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="320" />
                                </Grid.ColumnDefinitions>
                                <StackPanel Spacing="5" VerticalAlignment="Center">
                                    <TextBlock Style="{StaticResource SettingsLabelStyle}" Text="화면 모드" />
                                    <TextBlock Style="{StaticResource SettingsCaptionStyle}" Text="시스템, 라이트, 다크" />
                                </StackPanel>
                                <ComboBox
                                    x:Name="ThemeCombo"
                                    Grid.Column="1"
                                    Width="320"
                                    VerticalAlignment="Center"
                                    SelectionChanged="ThemeCombo_SelectionChanged" />
                            </Grid>
                        </Border>

                        <Border Style="{StaticResource SettingsCardStyle}">
                            <StackPanel Spacing="16">
                                <StackPanel Spacing="5">
                                    <TextBlock Style="{StaticResource SettingsLabelStyle}" Text="강조 컬러" />
                                    <TextBlock Style="{StaticResource SettingsCaptionStyle}" Text="선택 표시, 체크, 포커스 링에만 사용" />
                                </StackPanel>
                                <StackPanel x:Name="AccentSwatches" Orientation="Horizontal" Spacing="8" />
                            </StackPanel>
                        </Border>
                    </StackPanel>

                    <StackPanel
                        x:Name="NotificationsSection"
                        Spacing="12"
                        Visibility="Collapsed">
                        <TextBlock Style="{StaticResource SettingsSectionTitleStyle}" Text="알림" />

                        <Border Style="{StaticResource SettingsCardStyle}">
                            <Grid MinHeight="64" ColumnSpacing="20">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="320" />
                                </Grid.ColumnDefinitions>
                                <StackPanel Spacing="5" VerticalAlignment="Center">
                                    <TextBlock Style="{StaticResource SettingsLabelStyle}" Text="리마인더" />
                                    <TextBlock Style="{StaticResource SettingsCaptionStyle}" Text="리마인더 구현 단계에서 활성화" />
                                </StackPanel>
                                <ToggleSwitch
                                    Grid.Column="1"
                                    HorizontalAlignment="Right"
                                    VerticalAlignment="Center"
                                    IsEnabled="False" />
                            </Grid>
                        </Border>
                    </StackPanel>
                </Grid>
            </ScrollViewer>
        </Grid>
    </Grid>
</Page>
