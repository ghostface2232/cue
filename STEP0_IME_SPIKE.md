# 단계 0 — IME 조합 신호 스파이크: 실행 & 관찰 프로토콜

> **이것은 버리는 코드다.** `Pages/ImeSpikePage.xaml(.cs)`, `MainWindow.xaml`의 `IME 스파이크`
> 네비 항목, `MainWindow.xaml.cs`의 `imespike` 분기, 그리고 이 문서는 단계 0 판정이 끝나면
> 통째로 삭제한다. 위에 어떤 실제 기능도 쌓지 않는다.

## 왜 하는가

이 계획 전체의 트리거 설계는 세 가정 위에 선다. 코드를 더 짓기 전에 **Windows에서 실제 한글
IME로 직접 관찰**해 못 박는다.

1. **신호 존재**: `TextCompositionStarted/Changed/Ended`가 한글 IME에서 실제로 발화하고,
   "지금 조합 중인가"를 깨끗한 창으로 잡을 수 있는가?
2. **재틴트 안전**: commit/공백/멈춤 트리거에 전체 재틴트할 때 **caret이 튀는가**, **Ctrl+Z
   (native undo)** 스택이 깨지는가?
3. **leak 없음**: 조합 중 글자(`ㄱ→가→갈`)에 서식이 새는가?

## 실행

Windows(개발자 모드)에서 저장소 루트에서:

```powershell
.\BuildAndRun.ps1 Cue.csproj
```

앱이 뜨면 좌측 네비 하단 **"IME 스파이크"** 를 연다. 입력기를 **한글**로 바꾸고(한/영 키)
상단 RichEditBox에 타이핑하면서 **하단 로그**와 **상태 줄**(`IsComposing` / `caret` / `len` /
`compositions` / `lastTrigger`)을 본다. 디버거는 필요 없다 — 모든 신호가 화면에 찍힌다.

화면 토글:
- **비조합일 때만 재틴트**: 기본 ON. **끄면 조합 중에도 칠해 leak을 직접 관찰**한다.
- **틴트를 undo 그룹으로 격리**: ON/OFF를 바꿔 Ctrl+Z 거동을 비교한다.

---

## 관찰 시나리오 (순서대로)

### A. 조합 신호가 발화하는가 (가정 1)
1. 토글 둘 다 기본값(게이트 ON, undo 격리 ON).
2. `내일 오후 3시 회의` 를 천천히 친다.
3. **본다**: 글자마다 `CompositionStarted` → `CompositionChanged…` → `CompositionEnded`가
   로그에 찍히는가? 상태 줄 `IsComposing`이 조합 중 `true`, 글자 확정 후 `false`로 깔끔히
   토글되는가? `compositions` 카운트가 친 글자 수만큼 늘어나는가?
   - ✅ 찍힌다 → 신호 확보. 가정 1 통과.
   - ❌ 안 찍힌다 / `IsComposing`이 안 바뀐다 → 신호 없음. **대안 경로**(아래) 필요.

### B. caret이 튀는가 (가정 2-a)
1. `내일 오후 3시 회의` 입력 후, **문장 중간**(예: `오후`와 `3시` 사이)을 클릭해 caret을 둔다.
2. 끝에 공백을 하나 치거나 500ms 기다려 재틴트를 유발한다.
3. **본다**: 로그의 `tint […] caret a,b -> c,d` 줄. `-> ` 뒤 값이 앞 값과 같은가?
   `<<< CARET MOVED`가 뜨면 caret이 튄 것이다.
   - ✅ 안 튐 → 통과.
   - ⚠️ 튐 → 재틴트 전후 caret 저장·복원으로 잡히는지, 아니면 부분 재서식이 필요한지 기록.

### C. Ctrl+Z(native undo)가 깨지는가 (가정 2-b)
1. **undo 격리 ON** 상태로 `금요일 회의` 입력 → 재틴트가 돌게 둔다 → `Ctrl+Z`를 누른다.
2. **본다**: Ctrl+Z가 **내가 친 텍스트**를 되돌리는가(정상), 아니면 **틴트(색)** 만
   되돌리거나 아무 일도 안 일어나는가(오염)?
3. **undo 격리 OFF** 로 바꿔 같은 입력 반복, 거동을 비교한다.
   - ✅ 텍스트가 정상 되돌려짐 → 통과(어느 토글에서 정상인지 기록).
   - ⚠️ 색만 되돌려지거나 텍스트 undo가 망가짐 → 어느 토글에서 그런지 기록.

### D. 조합 글자에 서식이 새는가 (가정 3)
1. **게이트 ON**(기본)으로 `갈비` 처럼 조합이 여러 단계인 글자(`ㄱ→가→갈→갈비`)를 친다.
   조합 중에는 색이 안 들어가고, 확정(commit) 후에만 색이 정리되는가? → 기대 동작.
2. **게이트 OFF**(leak probe)로 같은 입력을 친다. 조합 중인 반쪽 글자에 액센트가 번지거나
   IME 조합이 깨지는가?
   - ✅ 게이트 ON에서 leak 없음 → 통과(게이트 설계가 유효).
   - ⚠️ 게이트 ON인데도 leak/깨짐 → 트리거를 commit 단위로만 더 좁혀야 함을 기록.

### E. `금요일 → 금요일마다` 전이(부수 확인)
1. `금요일` 입력 → 확정 후 색을 본다(throwaway 매처라 `금요일`/`요일`이 칠해짐) →
   이어서 `마다` 를 붙인다 → 재틴트 후 색이 갱신되는가?
   - 이 스파이크의 매처는 dumb하지만, **재틴트가 수정 지점에 매끄럽게 반응하는지**의
     감만 본다(정확한 kind 구분은 단계 1·3에서).

---

## 판정표 (채워서 회신)

| # | 가정 | 결과 | 메모 (어느 토글/입력에서) |
|---|------|------|--------------------------|
| 1 | Composition 이벤트 발화 + IsComposing 창 | ✅ / ❌ | |
| 2a | 재틴트 시 caret 안정 | ✅ / ⚠️ | |
| 2b | Ctrl+Z(native undo) 보존 | ✅ / ⚠️ | (격리 ON/OFF 각각) |
| 3 | 게이트 ON에서 조합 글자 leak 없음 | ✅ / ⚠️ | |
| - | 체감(깜빡임/지연 등 인상) | 자유 | |

## 판정 → 분기 (이 결과로 단계 1·2·3 경로 확정)

- **전부 ✅** → 단계 3의 **"full reset + re-tint" 단일 진실 경로**로 진행.
- **2a/2b ⚠️ (caret/undo 훼손)** → 단계 3을 **부분 재서식**(바뀐 토큰 span만 갱신)으로 설계.
- **3 ⚠️ (게이트로도 leak)** → 단계 2 트리거를 **IME commit 단위로만** 더 좁힘.
- **1 ❌ (신호 없음)** → 대안 신호 조사: `CoreTextEditContext` 직접 사용, 또는 조합 중
  중간 글자(자모 단위 변화)를 `TextChanged`로 식별. 확보 후에야 단계 2 트리거 재설계.

회신해 주시면 결과를 이 문서 하단에 박제하고, 스파이크 코드를 제거한 뒤 그 판정에 맞춰
단계 1(파서 계약 + 엔진 리팩터)로 들어가겠습니다.

---

---

## 1차 관찰 결과 (요약)

| # | 가정 | 결과 |
|---|------|------|
| 1 | Composition 이벤트 발화 + IsComposing 창 | ✅ 발화함 |
| 2a | 재틴트 시 caret 안정 | ✅ (보고된 튐 없음) |
| 2b | Ctrl+Z(native undo) 보존 | ❌ **프리징/심한 지연, 잘 안 지워짐** |
| 3 | 게이트 ON에서 조합 글자 leak 없음 | ✅ (leak 보고 없음) |

부수 관찰(둘 다 진짜 문제 아님):
- "다음 주" 미인식 → 스파이크의 **dumb 매처** 한계일 뿐. 실제 파서는 `다음주`/`다음 주`를
  인식함(`Korean.cs`의 `nextweek`). 신호 검증과 무관.
- "시간(3시/세시)이 띄어쓰기 후에만 색" → 한글 IME가 **마지막 음절을 조합 중으로 붙들기**
  때문. 게이트가 조합 글자를 칠하지 않는 건 올바른 동작이며, 공백/Enter 확정 시 칠해짐.

### 진단

원래 Tint는 **매 발화마다 문서 전체를 `CharacterFormat`으로 리셋+재색칠**했고, 그 서식 연산이
모두 native undo 스택에 쌓였다. Ctrl+Z가 "전체 재서식" 단계들을 되감으며 폭증 → 프리징.
이것이 단계 0 게이트가 미리 잡으려던 **load-bearing 리스크(2b)의 실제 발현**이다.

## v2 재검증 (스파이크 수정 후 — 프리징 원인 분리)

스파이크를 고쳤다(`ImeSpikePage.xaml.cs`):
- **diff-only 재틴트**: 전체 리셋을 버리고 **바뀐 span만** 재서식. 변화가 없으면 **undo 단위를
  아예 안 만든다**(토큰 내부를 타이핑하는 동안엔 undo 오염 0).
- **조합 prefix 틴트**: 조합 중에도 확정된 앞부분은 칠하되 활성 조합 꼬리는 절대 안 건드림 →
  앞 토큰들이 공백 없이 실시간으로 켜짐.

**재검증 순서** (다시 빌드 후 "IME 스파이크"):

1. **C 재현 (프리징):** `금요일 회의 내일 오후 3시` 정도를 친 뒤 `Ctrl+Z`를 여러 번.
   - **undo 격리 ON**과 **OFF** 각각에서 시도.
   - 본다: 여전히 프리징/지연인가? Ctrl+Z가 **텍스트**를 지우는가, 아니면 **색**만 먼저
     지우는가? 로그의 `tint […] +x/-y` 줄이 토큰 내부 타이핑 중엔 안 찍히는가(=undo 단위 0)?
2. **시간 토큰 라이브:** `내일 오후 3시 회의` 를 칠 때, 뒤 단어를 칠수록 **앞 토큰(`내일`,
   `오후`)이 공백 없이도** 주황색으로 켜지는가?

### v2 판정 → 분기

- **diff-only로 프리징이 사라지고, Ctrl+Z가 (색은 먼저 지워질 수 있으나) 텍스트를 정상
  되돌림** → RichEditBox 경로 **유지**, 단계 3을 **부분(diff) 재서식**으로 확정.
- **여전히 프리징하거나, Ctrl+Z가 텍스트를 못 지우고 색만 끝없이 되감김** → RichEditBox의
  서식이 undo 스택을 근본적으로 오염시킨다는 뜻. → **오버레이 경로로 분기**: 입력은 평문
  컨트롤에 두고, 강조는 텍스트 버퍼(=undo 대상)를 건드리지 않는 **별도 렌더 레이어**로 그린다.
  (이 경우 단계 2의 "RichEditBox 교체"를 "평문 + 오버레이"로 대체.)

→ 위 1·2 결과를 회신해 주세요. 그 결과로 단계 1~3의 경로(부분 재서식 vs 오버레이)를 확정합니다.

---

## 최종 결정 (Step 0 종결)

**경로: RichEditBox + 문서 CharacterFormat 틴트 (글자색).** 오버레이(배경칩)는 시각적으로
"따로 노는" 느낌 + 복합 표현에서 칩 과다로 어수선해 기각. Ctrl+Z가 색을 글자보다 먼저
되돌리는 한계는 남지만, 실사용에서 삭제는 대부분 Backspace/Delete이므로 **수용 가능한 트레이드오프**로 판단.

### 검증된 레시피 (실제 `OmniInputBox`가 그대로 구현할 것 — 스파이크에서 입증됨)

- **컨트롤**: 빠른입력 `TextBox` → `RichEditBox`. 배경 투명, placeholder 오버레이 유지,
  `IsSpellCheckEnabled=False`, plain-text 붙여넣기 강제, Enter→commit. VM `QuickAddText` 수동 브리지.
- **IME 게이트**: `TextCompositionStarted/Changed/Ended`로 `_isComposing` 추적. **조합 중 매
  키에는 절대 틴트하지 않는다.**
- **발화 트리거**: `(비조합 + 공백)` OR `(idle ~500ms — 조합 중에도 허용, 안정된 꼬리까지 포함해
  전체 칠)` OR `(조합 종료/commit)`. **재파싱·재틴트는 "보이는 텍스트가 실제로 바뀌었을 때만"**
  (`_lastText` 게이트) — 서식 적용이 비동기로 `TextChanged`를 재발생시켜 생기는 무한 루프 차단.
- **재틴트 = 전체 리셋**: 매번 전체 구간을 default 색으로 리셋 후 매치 span만 액센트. (diff-only는
  IME commit-reset + 서식 상속 때문에 불가.)
- **상속 깜빡임 차단**: 재틴트 후 caret이 zero-length면 `Selection.CharacterFormat`을 default로
  리셋 → 다음 타이핑 글자가 앞 액센트를 상속하지 않음.
- **재진입 가드**(`_tinting`)로 우리 서식 연산이 일으킨 `TextChanged` 무시.
- **깜빡임 억제**: `BatchDisplayUpdates`/`ApplyDisplayUpdates`. 틴트를 한 undo 단위로 묶으려면
  `BeginUndoGroup`/`EndUndoGroup`. **주의**: WinUI `ITextDocument`엔 `Undo(count)`가 없어
  서식을 undo 스택에서 빼는 TOM suspend는 불가 → Ctrl+Z 2단계는 구조적으로 잔존(수용).
- **테마**: `CharacterFormat`은 자동 반전 안 됨 → `ActualThemeChanged`에서 색 재적용.
- **토큰 출처**: 멍청한 정규식이 아니라 **단계 1의 실제 파서 토큰 계약**.

### 단계 1로 넘기는 설계 노트

- **과분할 금지(어수선함 방지)**: 인접한 같은 종류 토큰은 한 구간으로 합치고, 조사·기능어
  (`다음`, `주` 단독 등)는 강조에서 제외. 복합 표현(`다음 주 금요일 오후 3시`)이 칩/색 폭주로
  보이지 않게, 날짜구는 한 덩어리·반복만 시각 구분하는 방향을 단계 1 `QuickAddTokenKind`
  설계에 반영.

### 정리

스파이크 코드(`Pages/ImeSpikePage.*`, `MainWindow`의 `imespike` 네비 항목·분기)는 임무를
다해 제거한다(git 히스토리에 남음). 이 문서가 단계 2 입력 박스 구현의 스펙이 된다.

---

## 환경 메모

이 스파이크는 Windows에서만 빌드·실행된다(WinUI 3 / Windows App SDK, `net10.0-windows10.0.26100`,
개발자 모드 필요). 작성·커밋은 Linux 컨테이너에서 했으므로 **컴파일 검증은 하지 못했다** —
빌드가 막히면 오류 메시지를 회신해 주면 바로 고친다.
